<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- título de la vista -->
    <style name="labelTitle">
        <item name="android:gravity">center</item>
        <item name="android:padding">10dp</item>
        <item name="android:textSize">20dp</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:text">Mi cuenta</item>
        <item name="android:fontFamily">@font/roboto_regular</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <!-- estilo de los label de los formularios -->
    <style name="labelForm">
        <item name="android:fontFamily">@font/open_sans</item>
        <item name="android:textSize">16dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_marginBottom">10dp</item>
    </style>

    <!-- inputs -->
    <style name="input">
        <item name="android:background">@drawable/input_border</item>
        <item name="android:layout_height">35dp</item>
        <item name="android:layout_marginBottom">20dp</item>
    </style>

    <!-- botones -->
    <style name="button">
        <item name="android:fontFamily">@font/open_sans</item>
        <item name="android:background">@drawable/button</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:layout_width">200dp</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:layout_height">40dp</item>>
        <item name="android:layout_gravity">center_horizontal</item>
    </style>

    <!-- botones que tienen la misma apariencia que si fueran un link -->
    <style name="fakeLink">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:inputType">textNoSuggestions</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center_horizontal</item>
    </style>

</resources>